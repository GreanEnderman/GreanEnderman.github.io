# 博客项目使用指南

这是一个现代化的个人博客项目，采用 Bento Grid（便当盒风格）设计，支持 GitHub Pages 静态托管。

## 📖 项目功能

### 核心功能
- ✅ **Bento Grid 布局首页** - 采用流行的网格卡片设计
- ✅ **明暗主题切换** - 支持浅色/深色模式，自动跟随系统设置
- ✅ **MDX 博客系统** - 使用 MDX 格式撰写文章
- ✅ **响应式设计** - 完美适配桌面和移动设备
- ✅ **静态导出** - 专为 GitHub Pages 优化，无需服务器
- ✅ **流畅动画** - 使用 Framer Motion 实现精美过渡效果

### 首页模块
1. **个人简介卡片** - 展示头像、姓名、职位和个人简介
2. **社交链接** - GitHub、Twitter、LinkedIn、邮箱等链接
3. **最新文章** - 显示最新的 3 篇博客文章
4. **技术栈展示** - 按类别展示你的技术技能
5. **正在阅读/聆听** - 展示当前正在阅读的书籍和音乐
6. **主题切换按钮** - 一键切换明暗主题

## 🚀 快速开始

### 1. 安装依赖
```bash
npm install
```

### 2. 启动开发服务器
```bash
npm run dev
```

访问 http://localhost:3000 即可查看博客

### 3. 构建生产版本
```bash
npm run build
```

构建完成后，静态文件会生成在 `out/` 目录中。

### 4. 清理缓存（如果遇到问题）
```bash
npm run clean
```

如果遇到页面错误或路由问题，运行此命令清理 Next.js 缓存，然后重新启动开发服务器。

## 📝 编辑页面内容

### 修改个人信息

#### 编辑个人简介
打开文件 `components/bento/ProfileCard.tsx`，修改以下内容：

```tsx
<CardTitle>你的名字</CardTitle>
<p className="text-sm text-muted-foreground">你的职位</p>

<p className="text-sm text-muted-foreground mb-4">
  在这里写你的个人简介...
</p>

<Badge variant="secondary">自定义状态</Badge>
```

#### 修改社交链接
打开文件 `components/bento/SocialLinks.tsx`，找到 `socialLinks` 数组并修改：

```tsx
const socialLinks = [
  {
    name: "GitHub",
    icon: Github,
    href: "https://github.com/你的用户名",
    color: "hover:bg-black/5 dark:hover:bg-white/5"
  },
  {
    name: "Twitter",
    icon: Twitter,
    href: "https://twitter.com/你的用户名",
    color: "hover:bg-sky-500/10"
  },
  // 添加更多社交链接...
];
```

#### 修改技术栈
打开文件 `components/bento/TechStack.tsx`，编辑 `techStack` 数组：

```tsx
const techStack = [
  { name: "TypeScript", category: "Languages" },
  { name: "React", category: "Frontend" },
  { name: "你的技术", category: "分类名称" },
  // 添加更多技能...
];
```

#### 修改阅读/音乐状态
打开文件 `components/bento/NowReading.tsx`：

```tsx
<h4 className="font-semibold">Reading</h4>
<p className="text-sm text-muted-foreground">
  "书名"
</p>
<p className="text-xs text-muted-foreground mt-1">
  by 作者名
</p>

<h4 className="font-semibold">Listening</h4>
<p className="text-sm text-muted-foreground">
  音乐/播客名称
</p>
<p className="text-xs text-muted-foreground mt-1">
  Spotify / 其他平台
</p>
```

### 修改网站主题颜色

打开文件 `app/globals.css`，找到 CSS 变量定义部分：

```css
:root {
  --background: 0 0% 100%;      /* 背景色 */
  --foreground: 222.2 84% 4.9%;  /* 前景色（文字） */
  --primary: 222.2 47.4% 11.2%;  /* 主色调 */
  --border: 214.3 31.8% 91.4%;   /* 边框色 */
  /* ... 更多颜色变量 */
}

.dark {
  --background: 222.2 84% 4.9%;  /* 深色模式背景 */
  --foreground: 210 40% 98%;      /* 深色模式文字 */
  /* ... 更多深色模式颜色 */
}
```

这些颜色使用 HSL 格式，你可以根据需要调整。

## ✍️ 文章管理

### 文章格式

所有文章存放在 `content/posts/` 目录下，使用 `.mdx` 扩展名。

文章模板：

```mdx
---
title: "文章标题"
date: "2024-01-15"
excerpt: "文章简介，会显示在首页文章列表中"
tags: ["标签1", "标签2", "标签3"]
---

# 文章正文

这里是文章内容，支持标准 Markdown 语法和 JSX 组件。
```

### 添加新文章

1. 在 `content/posts/` 目录下创建新文件，例如：`my-new-post.mdx`
2. 添加 Frontmatter（文件开头的 YAML 元数据）：

```mdx
---
title: "我的新文章"
date: "2024-01-20"
excerpt: "这是一篇关于...的文章"
tags: ["技术", "教程"]
---

# 文章标题

文章内容...
```

3. 保存文件后，文章会自动出现在首页的"最新文章"列表中

### 编辑文章

直接编辑 `content/posts/` 目录下对应的 `.mdx` 文件即可。

### 删除文章

从 `content/posts/` 目录中删除对应的 `.mdx` 文件。

**注意**：删除文件后，记得运行 `npm run build` 重新构建，以更新静态页面。

### MDX 语法支持

你可以在文章中使用：

- 标准 Markdown 语法（标题、列表、引用等）
- 代码块（支持语法高亮）
- 图片、链接
- JSX 组件（如果需要）

示例：

```mdx
---
title: "MDX 示例"
date: "2024-01-15"
---

# 欢迎使用 MDX

## 代码示例

```javascript
function hello() {
  console.log("Hello, World!");
}
```

## 列表

- 项目 1
- 项目 2
- 项目 3

## 引用

> 这是一段引用文本

## 链接

访问 [GitHub](https://github.com)
```

## 🌐 部署到 GitHub Pages

### 方法一：手动部署

#### 1. 构建项目
```bash
npm run build
```

这会在项目根目录生成 `out/` 文件夹，包含所有静态文件。

#### 2. 推送到 GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/你的用户名/你的仓库名.git
git push -u origin main
```

#### 3. 配置 GitHub Pages

1. 进入你的 GitHub 仓库
2. 点击 **Settings**（设置）
3. 在左侧菜单找到 **Pages**
4. 在 **Source** 下拉菜单中选择：
   - Branch: `main`
   - Folder: `/root` 或 `/ (root)`
5. 点击 **Save**（保存）

#### 6. 访问你的博客

几分钟后，你的博客将发布在：
```
https://你的用户名.github.io/你的仓库名/
```

### 方法二：使用 GitHub Actions 自动部署

创建 `.github/workflows/deploy.yml` 文件：

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: './out'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

#### 配置 GitHub Pages 使用 Actions

1. 在仓库的 **Settings** -> **Pages** 中
2. 在 **Source** 下选择 **GitHub Actions**
3. 保存设置

现在每次你推送代码到 `main` 分支时，GitHub Actions 会自动构建并部署你的博客。

### 自定义域名（可选）

如果你想使用自定义域名：

1. 在 `app/` 目录下创建 `public/` 文件夹（如果不存在）
2. 在 `public/` 目录中创建 `CNAME` 文件
3. 在文件中写入你的域名（例如：`blog.yourdomain.com`）
4. 在你的域名提供商处配置 DNS 记录
5. 在 GitHub 仓库的 **Settings** -> **Pages** 中添加自定义域名

## 🎨 常见自定义需求

### 修改网站标题和描述

编辑 `app/layout.tsx`：

```tsx
export const metadata: Metadata = {
  title: "你的网站标题",
  description: "你的网站描述",
};
```

### 添加 Favicon

1. 将 favicon 文件（如 `favicon.ico`）放入 `app/` 或 `public/` 目录
2. Next.js 会自动识别它

### 修改字体

在 `app/layout.tsx` 中修改字体：

```tsx
import { Inter, Geist_Sans } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });
// 或使用 Geist Sans
const geistSans = Geist_Sans({
  subsets: ["latin"],
  variable: "--font-geist-sans",
});
```

然后在 `<body>` 标签中应用：

```tsx
<body className={inter.className}>
```

## 📦 项目结构说明

```
blog/
├── app/                          # Next.js App Router 目录
│   ├── layout.tsx               # 根布局（包含主题配置）
│   ├── page.tsx                 # 首页（Bento Grid）
│   ├── globals.css              # 全局样式和主题变量
│   └── posts/                   # 博客文章页面
│       └── [slug]/              # 动态路由
│           └── page.tsx         # 文章详情页
│
├── components/                  # React 组件
│   ├── bento/                   # Bento Grid 模块
│   │   ├── ProfileCard.tsx     # 个人简介卡片
│   │   ├── SocialLinks.tsx     # 社交链接
│   │   ├── PostList.tsx        # 文章列表
│   │   ├── TechStack.tsx       # 技术栈
│   │   └── NowReading.tsx      # 阅读/音乐状态
│   ├── ui/                      # 基础 UI 组件
│   │   ├── card.tsx            # 卡片组件
│   │   ├── badge.tsx           # 标签组件
│   │   └── button.tsx          # 按钮组件
│   ├── ThemeProvider.tsx        # 主题提供者
│   └── ThemeToggle.tsx          # 主题切换按钮
│
├── content/
│   └── posts/                   # MDX 博客文章
│       ├── getting-started-nextjs.mdx
│       ├── typescript-generics.mdx
│       └── tailwind-css-guide.mdx
│
├── lib/                         # 工具函数
│   ├── utils.ts                # 通用工具函数
│   └── posts.ts                # 文章处理函数
│
├── public/                      # 静态资源（图片等）
├── next.config.mjs             # Next.js 配置
├── tailwind.config.ts          # Tailwind CSS 配置
├── package.json                # 依赖配置
└── tsconfig.json               # TypeScript 配置
```

## 🔧 开发建议

### 本地开发
```bash
npm run dev          # 启动开发服务器（支持热重载）
npm run build        # 构建生产版本
npm run start        # 启动生产服务器（需要先构建）
npm run lint         # 代码检查
npm run clean        # 清理 Next.js 缓存（解决路由问题）
```

### 添加新功能

1. **新建 Bento 模块**：在 `components/bento/` 下创建新组件
2. **添加到首页**：在 `app/page.tsx` 中导入并使用新组件
3. **保持样式一致**：使用现有的 UI 组件（Card、Badge 等）

### 性能优化建议

- 图片已配置为 `unoptimized: true`，适合 GitHub Pages
- 所有页面在构建时预渲染为静态 HTML
- 使用 `generateStaticParams` 为所有文章生成静态页面

## ❓ 常见问题

### Q: 为什么更改文章后看不到更新？
A: 开发模式下应该自动刷新。如果是生产环境，需要重新运行 `npm run build`。

### Q: 如何添加图片到文章？
A: 将图片放入 `public/` 目录，然后在 MDX 中引用：`![图片描述](/图片名.jpg)`

### Q: GitHub Pages 显示 404？
A: 检查：
- 构建是否成功（`npm run build`）
- GitHub Pages 设置是否正确
- 是否将 `out/` 目录推送到仓库

### Q: 主题切换不工作？
A: 确保 `app/layout.tsx` 中包含 `ThemeProvider`，并且 `app/globals.css` 中的主题变量正确配置。

### Q: 遇到 "Page is missing param in generateStaticParams()" 错误？
A: 这是 Next.js 缓存问题。解决方法：
1. 停止开发服务器（Ctrl + C）
2. 运行 `npm run clean` 清理缓存
3. 重新运行 `npm run dev` 启动开发服务器

如果问题仍然存在，手动删除以下文件夹：
- `.next/`
- `out/`
- `node_modules/.cache/`

### Q: 开发服务器启动报错？
A: 尝试以下步骤：
1. 删除 `node_modules` 文件夹和 `package-lock.json`
2. 运行 `npm install` 重新安装依赖
3. 运行 `npm run clean` 清理缓存
4. 重新启动 `npm run dev`

## 📚 技术栈说明

- **Next.js 14+** - React 框架，使用 App Router
- **TypeScript** - 类型安全
- **Tailwind CSS** - 实用优先的 CSS 框架
- **Framer Motion** - 动画库
- **next-themes** - 主题切换
- **MDX** - Markdown + JSX
- **gray-matter** - 解析 Frontmatter
- **Lucide React** - 图标库

## 🤝 获取帮助

如果遇到问题：
1. 检查本文档的常见问题部分
2. 查看 Next.js 官方文档：https://nextjs.org/docs
3. 查看各依赖库的文档

## 📄 许可证

本项目为个人博客模板，可自由修改和使用。

---

**祝你使用愉快！** 🎉
