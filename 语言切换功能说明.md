# 中英文语言切换功能说明

## ✨ 功能概述

博客现已支持中英文双语切换，用户可以通过点击语言切换按钮在中文和英文之间自由切换。

## 🎯 实现的功能

### 1. 语言切换按钮
- 位置：首页右上角，主题切换按钮旁边
- 图标：地球图标（Languages）
- 功能：点击切换中英文

### 2. 已翻译的内容

#### 首页
- ✅ 欢迎标题和副标题
- ✅ 个人简介卡片（姓名、职位、简介、状态）
- ✅ 社交链接标题
- ✅ 最新文章标题
- ✅ 技术栈标题和分类
- ✅ 正在阅读/聆听模块

#### 文章详情页
- ✅ "返回首页"按钮文字

### 3. 翻译文件位置
所有翻译内容存储在 `lib/translations.ts` 文件中，包含：
- 英文翻译对象 (`en`)
- 中文翻译对象 (`zh`)

## 🔧 如何添加/修改翻译

### 修改现有翻译

编辑 `lib/translations.ts` 文件：

```typescript
export const translations: Translations = {
  en: {
    header: {
      welcome: "Welcome",  // 修改这里
      subtitle: "My personal space on the web",
    },
    // ...
  },
  zh: {
    header: {
      welcome: "欢迎",  // 修改这里
      subtitle: "我在网络上的个人空间",
    },
    // ...
  },
};
```

### 添加新的翻译文本

#### 步骤 1: 在翻译文件中添加键值对

```typescript
// lib/translations.ts
export const translations: Translations = {
  en: {
    // 现有翻译...
    myNewSection: {
      title: "New Title",
      description: "New Description",
    },
  },
  zh: {
    // 现有翻译...
    myNewSection: {
      title: "新标题",
      description: "新描述",
    },
  },
};
```

#### 步骤 2: 在组件中使用翻译

```tsx
"use client";

import { useLanguage } from "@/components/LanguageProvider";

export function MyComponent() {
  const { t } = useLanguage();

  return (
    <div>
      <h1>{t("myNewSection.title")}</h1>
      <p>{t("myNewSection.description")}</p>
    </div>
  );
}
```

## 📁 相关文件结构

```
blog/
├── components/
│   ├── LanguageProvider.tsx     # 语言上下文提供者
│   ├── LanguageToggle.tsx       # 语言切换按钮组件
│   ├── Home.tsx                 # 首页客户端组件
│   ├── PostContent.tsx          # 文章内容客户端组件
│   └── bento/
│       ├── ProfileCard.tsx      # 使用 useLanguage()
│       ├── SocialLinks.tsx      # 使用 useLanguage()
│       ├── PostList.tsx         # 使用 useLanguage()
│       ├── TechStack.tsx        # 使用 useLanguage()
│       └── NowReading.tsx       # 使用 useLanguage()
├── lib/
│   └── translations.ts          # 翻译文件
├── types/
│   └── i18n.ts                  # TypeScript 类型定义
└── app/
    ├── layout.tsx               # 包含 LanguageProvider
    ├── page.tsx                 # 服务器组件，调用 Home
    └── posts/[slug]/
        └── page.tsx             # 服务器组件，调用 PostContent
```

## 🎨 自定义个人信息

### 修改中文个人信息

编辑 `lib/translations.ts` 中的 `zh.profile` 部分：

```typescript
zh: {
  profile: {
    name: "张三",           // 你的名字
    title: "全栈开发者",     // 你的职位
    bio: "热衷于使用现代技术构建优美的 Web 体验。",  // 你的简介
    status: "正在找工作",    // 你的状态
  },
}
```

### 修改英文个人信息

编辑 `lib/translations.ts` 中的 `en.profile` 部分：

```typescript
en: {
  profile: {
    name: "John Doe",
    title: "Full Stack Developer",
    bio: "Passionate about building beautiful web experiences with modern technologies.",
    status: "Available for work",
  },
}
```

## 🚀 使用方法

### 切换语言
1. 启动博客：`npm run dev`
2. 打开 http://localhost:3000
3. 点击右上角的语言切换按钮（地球图标）
4. 页面会立即切换到另一种语言

### 默认语言设置

在 `app/layout.tsx` 中修改默认语言：

```tsx
<LanguageProvider defaultLanguage="zh">  {/* 或 "en" */}
  <ThemeProvider>
    {children}
  </ThemeProvider>
</LanguageProvider>
```

## 🌐 技术实现

### 使用 React Context
- `LanguageProvider` 提供全局语言状态
- `useLanguage()` Hook 在任何组件中访问翻译函数

### 翻译函数
```typescript
const { t } = useLanguage();
t("header.welcome")  // 返回当前语言的 "欢迎" 或 "Welcome"
```

### 嵌套翻译键
支持点号语法访问嵌套对象：
```typescript
t("techStack.categories.Languages")  // 返回 "编程语言" 或 "Languages"
```

## 💡 注意事项

1. **客户端组件**：使用 `useLanguage()` 的组件必须是客户端组件（添加 `"use client"` 指令）

2. **服务器组件**：服务器组件可以通过 props 传递数据给客户端组件

3. **翻译缺失**：如果某个翻译键不存在，会返回键名本身

4. **持久化**：当前语言存储在 React 状态中，刷新页面会重置为默认语言

## 🔮 未来改进建议

如果需要持久化语言选择或支持更多语言，可以考虑：

1. **使用 localStorage**：保存用户的语言偏好
2. **URL 参数**：通过 URL 参数（如 `/zh` 或 `/en`）设置语言
3. **Accept-Language 头**：根据浏览器语言自动选择
4. **专业 i18n 库**：考虑使用 `next-intl` 或 `react-i18next` 等成熟库

## 📝 示例：为新组件添加多语言支持

假设你要添加一个新的"项目展示"模块：

### 1. 添加翻译
```typescript
// lib/translations.ts
export const translations: Translations = {
  en: {
    projects: {
      title: "My Projects",
      subtitle: "Some of my recent work",
      viewProject: "View Project",
    },
  },
  zh: {
    projects: {
      title: "我的项目",
      subtitle: "我最近的一些作品",
      viewProject: "查看项目",
    },
  },
};
```

### 2. 创建组件
```tsx
// components/bento/Projects.tsx
"use client";

import { useLanguage } from "@/components/LanguageProvider";

export function Projects() {
  const { t } = useLanguage();

  return (
    <div>
      <h2>{t("projects.title")}</h2>
      <p>{t("projects.subtitle")}</p>
      <button>{t("projects.viewProject")}</button>
    </div>
  );
}
```

就这么简单！✨

---

**现在你的博客已经完全支持中英文双语了！** 🎉
